<p id="notice"><%= notice %></p>

<h1>
  <strong><%= @beer_club.name %> <%= @beer_club.city %></strong>
</h1>

<table>
  <% @users = BeerClub.users %>

  <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user_path %></td>
      </tr>
  <% end %>
</table>

<%= link_to 'Edit', edit_beer_club_path(@beer_club) %> |
<%= link_to 'Back', beer_clubs_path %>
